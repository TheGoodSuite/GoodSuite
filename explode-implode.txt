

explode <:-
implode <EXPLOSION MARK(random id)*><:-
explode -:>
implode -:><EXPLOSION MARK(random od)
explode <:
implode <EXPLOSION MARK(random id)*><:
explode :>
implode :><EXPLOSION MARK(random od)

explode <*EXPLOSION MARK(random id)*>